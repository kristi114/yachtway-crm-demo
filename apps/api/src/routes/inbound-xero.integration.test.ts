import { describe, it, expect } from "vitest";

// PLACEHOLDER 2026-07-27 — inbound Xero handlers removed. The payment-rollup +
// partner-settlement logic is rebuilt CRM-native in Accounting Phases A1/A3, with
// new integration tests there. Keeps the runner green in the meantime.
describe("inbound accounting (rebuilding — Accounting A1/A3)", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
