import { describe, it, expect } from "vitest";

// PLACEHOLDER 2026-07-27 — studio itemized/multi-opp invoicing is being rebuilt CRM-native
// (Xero removed; catalog no longer synced from Xero). Real integration tests are rebuilt in
// Accounting Phase A1/A4. Keeps the runner green in the meantime.
describe("studio invoices (rebuilding — Accounting A1)", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
